/// Core-only method names. Feature-specific names live in each feature's
/// `method_names.dart` to avoid a single stale registry.
class MethodNames {
  const MethodNames._();

  static const String getPlatformVersion = 'getPlatformVersion';
}
